{
  "openapi": "3.0.2",
  "info": { "title": "Fixture", "version": "revision" },
  "paths": {
    "/widgets": {
      "get": {
        "parameters": [
          {
            "name": "token",
            "in": "query",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": { "200": { "description": "200" } }
      }
    }
  }
}
